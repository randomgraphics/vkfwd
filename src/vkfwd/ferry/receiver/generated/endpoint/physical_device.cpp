#include "generated/endpoint/physical_device.hpp"

#include "generated/command/vkEnumeratePhysicalDevices.hpp"
#include "generated/command/vkGetPhysicalDeviceProperties.hpp"
#include "generated/command/vkGetPhysicalDeviceFeatures.hpp"
#include "generated/command/vkGetPhysicalDeviceQueueFamilyProperties.hpp"
#include "generated/command/vkGetPhysicalDeviceMemoryProperties.hpp"
#include "generated/command/vkEnumerateDeviceExtensionProperties.hpp"
#include "generated/receiver_hooks.hpp"
#include "memory_map_manager.hpp"

// Generated by src/vkfwd/ferry/script/generator/vulkan_metadata.py; do not edit by hand.
// Vulkan API version: 1.4.352
// Vulkan XML SHA256: 50e66c781e8afb9c80ffae10e3f7579f71afae6f9e77f22d50eeb963b3939482

#if __has_include("hook/vkEnumeratePhysicalDevicesReceiverHook.hpp")
    #include "hook/vkEnumeratePhysicalDevicesReceiverHook.hpp"
#endif
#if __has_include("hook/vkGetPhysicalDevicePropertiesReceiverHook.hpp")
    #include "hook/vkGetPhysicalDevicePropertiesReceiverHook.hpp"
#endif
#if __has_include("hook/vkGetPhysicalDeviceFeaturesReceiverHook.hpp")
    #include "hook/vkGetPhysicalDeviceFeaturesReceiverHook.hpp"
#endif
#if __has_include("hook/vkGetPhysicalDeviceQueueFamilyPropertiesReceiverHook.hpp")
    #include "hook/vkGetPhysicalDeviceQueueFamilyPropertiesReceiverHook.hpp"
#endif
#if __has_include("hook/vkGetPhysicalDeviceMemoryPropertiesReceiverHook.hpp")
    #include "hook/vkGetPhysicalDeviceMemoryPropertiesReceiverHook.hpp"
#endif
#if __has_include("hook/vkEnumerateDeviceExtensionPropertiesReceiverHook.hpp")
    #include "hook/vkEnumerateDeviceExtensionPropertiesReceiverHook.hpp"
#endif

namespace vkfwd::receiver::generated {

bool vkEnumeratePhysicalDevices_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                         ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkEnumeratePhysicalDevices::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::EnumeratePhysicalDevices>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::EnumeratePhysicalDevices);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkEnumeratePhysicalDevices>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->instance, parameters->pPhysicalDeviceCount, parameters->pPhysicalDevices);
        Command::Response response {.return_value         = return_value,
                                    .pPhysicalDeviceCount = parameters->pPhysicalDeviceCount,
                                    .pPhysicalDevices     = parameters->pPhysicalDevices};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkGetPhysicalDeviceProperties_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                            ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkGetPhysicalDeviceProperties::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::GetPhysicalDeviceProperties>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::GetPhysicalDeviceProperties);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkGetPhysicalDeviceProperties>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->physicalDevice, parameters->pProperties);
        Command::Response response {.pProperties = parameters->pProperties};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkGetPhysicalDeviceFeatures_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                          ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkGetPhysicalDeviceFeatures::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::GetPhysicalDeviceFeatures>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::GetPhysicalDeviceFeatures);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkGetPhysicalDeviceFeatures>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->physicalDevice, parameters->pFeatures);
        Command::Response response {.pFeatures = parameters->pFeatures};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkGetPhysicalDeviceQueueFamilyProperties_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                                       ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkGetPhysicalDeviceQueueFamilyProperties::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::GetPhysicalDeviceQueueFamilyProperties>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::GetPhysicalDeviceQueueFamilyProperties);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkGetPhysicalDeviceQueueFamilyProperties>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->physicalDevice, parameters->pQueueFamilyPropertyCount, parameters->pQueueFamilyProperties);
        Command::Response response {.pQueueFamilyPropertyCount = parameters->pQueueFamilyPropertyCount,
                                    .pQueueFamilyProperties    = parameters->pQueueFamilyProperties};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkGetPhysicalDeviceMemoryProperties_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                                  ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkGetPhysicalDeviceMemoryProperties::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::GetPhysicalDeviceMemoryProperties>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::GetPhysicalDeviceMemoryProperties);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkGetPhysicalDeviceMemoryProperties>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->physicalDevice, parameters->pMemoryProperties);
        Command::Response response {.pMemoryProperties = parameters->pMemoryProperties};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkEnumerateDeviceExtensionProperties_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                                   ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkEnumerateDeviceExtensionProperties::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::EnumerateDeviceExtensionProperties>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::EnumerateDeviceExtensionProperties);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkEnumerateDeviceExtensionProperties>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->physicalDevice, parameters->pLayerName, parameters->pPropertyCount, parameters->pProperties);
        Command::Response response {.return_value = return_value, .pPropertyCount = parameters->pPropertyCount, .pProperties = parameters->pProperties};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

} // namespace vkfwd::receiver::generated
