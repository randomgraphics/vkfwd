#include "generated/endpoint/device_lifecycle.hpp"

#include "generated/command/vkCreateDevice.hpp"
#include "generated/command/vkDestroyDevice.hpp"
#include "generated/command/vkGetDeviceQueue.hpp"
#include "generated/command/vkDeviceWaitIdle.hpp"
#include "generated/receiver_hooks.hpp"
#include "memory_map_manager.hpp"

// Generated by src/vkfwd/ferry/script/generator/vulkan_metadata.py; do not edit by hand.
// Vulkan API version: 1.4.352
// Vulkan XML SHA256: 50e66c781e8afb9c80ffae10e3f7579f71afae6f9e77f22d50eeb963b3939482

#if __has_include("hook/vkCreateDeviceReceiverHook.hpp")
    #include "hook/vkCreateDeviceReceiverHook.hpp"
#endif
#if __has_include("hook/vkDestroyDeviceReceiverHook.hpp")
    #include "hook/vkDestroyDeviceReceiverHook.hpp"
#endif
#if __has_include("hook/vkGetDeviceQueueReceiverHook.hpp")
    #include "hook/vkGetDeviceQueueReceiverHook.hpp"
#endif
#if __has_include("hook/vkDeviceWaitIdleReceiverHook.hpp")
    #include "hook/vkDeviceWaitIdleReceiverHook.hpp"
#endif

namespace vkfwd::receiver::generated {

bool vkCreateDevice_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                             ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkCreateDevice::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::CreateDevice>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::CreateDevice);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkCreateDevice>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->physicalDevice, parameters->pCreateInfo, parameters->pAllocator, parameters->pDevice);
        Command::Response response {.return_value = return_value, .pDevice = parameters->pDevice};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkDestroyDevice_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                              ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkDestroyDevice::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::DestroyDevice>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::DestroyDevice);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkDestroyDevice>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->pAllocator);
        return true;
    }
}

bool vkGetDeviceQueue_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                               ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkGetDeviceQueue::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::GetDeviceQueue>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::GetDeviceQueue);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkGetDeviceQueue>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->queueFamilyIndex, parameters->queueIndex, parameters->pQueue);
        Command::Response response {.pQueue = parameters->pQueue};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkDeviceWaitIdle_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                               ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkDeviceWaitIdle::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::DeviceWaitIdle>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::DeviceWaitIdle);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkDeviceWaitIdle>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device);
        Command::Response response {.return_value = return_value};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

} // namespace vkfwd::receiver::generated
