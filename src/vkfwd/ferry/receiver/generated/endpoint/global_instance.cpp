#include "generated/endpoint/global_instance.hpp"

#include "generated/command/vkEnumerateInstanceVersion.hpp"
#include "generated/command/vkEnumerateInstanceLayerProperties.hpp"
#include "generated/command/vkEnumerateInstanceExtensionProperties.hpp"
#include "generated/command/vkCreateInstance.hpp"
#include "generated/command/vkDestroyInstance.hpp"
#include "generated/receiver_hooks.hpp"
#include "memory_map_manager.hpp"

// Generated by src/vkfwd/ferry/script/generator/vulkan_metadata.py; do not edit by hand.
// Vulkan API version: 1.4.352
// Vulkan XML SHA256: 50e66c781e8afb9c80ffae10e3f7579f71afae6f9e77f22d50eeb963b3939482

#if __has_include("hook/vkEnumerateInstanceVersionReceiverHook.hpp")
    #include "hook/vkEnumerateInstanceVersionReceiverHook.hpp"
#endif
#if __has_include("hook/vkEnumerateInstanceLayerPropertiesReceiverHook.hpp")
    #include "hook/vkEnumerateInstanceLayerPropertiesReceiverHook.hpp"
#endif
#if __has_include("hook/vkEnumerateInstanceExtensionPropertiesReceiverHook.hpp")
    #include "hook/vkEnumerateInstanceExtensionPropertiesReceiverHook.hpp"
#endif
#if __has_include("hook/vkCreateInstanceReceiverHook.hpp")
    #include "hook/vkCreateInstanceReceiverHook.hpp"
#endif
#if __has_include("hook/vkDestroyInstanceReceiverHook.hpp")
    #include "hook/vkDestroyInstanceReceiverHook.hpp"
#endif

namespace vkfwd::receiver::generated {

bool vkEnumerateInstanceVersion_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                         ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkEnumerateInstanceVersion::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::EnumerateInstanceVersion>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::EnumerateInstanceVersion);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkEnumerateInstanceVersion>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->pApiVersion);
        Command::Response response {.return_value = return_value, .pApiVersion = parameters->pApiVersion};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkEnumerateInstanceLayerProperties_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                                 ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkEnumerateInstanceLayerProperties::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::EnumerateInstanceLayerProperties>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::EnumerateInstanceLayerProperties);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkEnumerateInstanceLayerProperties>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->pPropertyCount, parameters->pProperties);
        Command::Response response {.return_value = return_value, .pPropertyCount = parameters->pPropertyCount, .pProperties = parameters->pProperties};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkEnumerateInstanceExtensionProperties_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                                     ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkEnumerateInstanceExtensionProperties::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::EnumerateInstanceExtensionProperties>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::EnumerateInstanceExtensionProperties);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkEnumerateInstanceExtensionProperties>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->pLayerName, parameters->pPropertyCount, parameters->pProperties);
        Command::Response response {.return_value = return_value, .pPropertyCount = parameters->pPropertyCount, .pProperties = parameters->pProperties};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkCreateInstance_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                               ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkCreateInstance::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::CreateInstance>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::CreateInstance);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkCreateInstance>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->pCreateInfo, parameters->pAllocator, parameters->pInstance);
        Command::Response response {.return_value = return_value, .pInstance = parameters->pInstance};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkDestroyInstance_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkDestroyInstance::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::DestroyInstance>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::DestroyInstance);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkDestroyInstance>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->instance, parameters->pAllocator);
        return true;
    }
}

} // namespace vkfwd::receiver::generated
