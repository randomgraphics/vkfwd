#include "generated/endpoint/renderpass_framebuffer.hpp"

#include "generated/command/vkCreateRenderPass.hpp"
#include "generated/command/vkDestroyRenderPass.hpp"
#include "generated/receiver_hooks.hpp"
#include "memory_map/manager.hpp"

// Generated by src/vkfwd/ferry/script/generator/vulkan_metadata.py; do not edit by hand.
// Vulkan API version: 1.4.352
// Vulkan XML SHA256: 50e66c781e8afb9c80ffae10e3f7579f71afae6f9e77f22d50eeb963b3939482

#if __has_include("hook/vkCreateRenderPassReceiverHook.hpp")
    #include "hook/vkCreateRenderPassReceiverHook.hpp"
#endif
#if __has_include("hook/vkDestroyRenderPassReceiverHook.hpp")
    #include "hook/vkDestroyRenderPassReceiverHook.hpp"
#endif

namespace vkfwd::receiver::generated {

bool vkCreateRenderPass_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                 ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkCreateRenderPass::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::CreateRenderPass>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::CreateRenderPass);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkCreateRenderPass>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device, parameters->pCreateInfo, parameters->pAllocator, parameters->pRenderPass);
        Command::Response response {.return_value = return_value, .pRenderPass = parameters->pRenderPass};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkDestroyRenderPass_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                  ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkDestroyRenderPass::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::DestroyRenderPass>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::DestroyRenderPass);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkDestroyRenderPass>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->renderPass, parameters->pAllocator);
        return true;
    }
}

} // namespace vkfwd::receiver::generated
