#include "generated/endpoint/memory_buffer.hpp"

#include "generated/command/vkCreateBuffer.hpp"
#include "generated/command/vkDestroyBuffer.hpp"
#include "generated/command/vkGetBufferMemoryRequirements.hpp"
#include "generated/command/vkAllocateMemory.hpp"
#include "generated/command/vkFreeMemory.hpp"
#include "generated/command/vkBindBufferMemory.hpp"
#include "generated/command/vkMapMemory.hpp"
#include "generated/command/vkUnmapMemory.hpp"
#include "generated/command/vkFlushMappedMemoryRanges.hpp"
#include "generated/command/vkInvalidateMappedMemoryRanges.hpp"
#include "generated/receiver_hooks.hpp"
#include "memory_map/manager.hpp"

// Generated by src/vkfwd/ferry/script/generator/vulkan_metadata.py; do not edit by hand.
// Vulkan API version: 1.4.352
// Vulkan XML SHA256: 50e66c781e8afb9c80ffae10e3f7579f71afae6f9e77f22d50eeb963b3939482

#if __has_include("hook/vkCreateBufferReceiverHook.hpp")
    #include "hook/vkCreateBufferReceiverHook.hpp"
#endif
#if __has_include("hook/vkDestroyBufferReceiverHook.hpp")
    #include "hook/vkDestroyBufferReceiverHook.hpp"
#endif
#if __has_include("hook/vkGetBufferMemoryRequirementsReceiverHook.hpp")
    #include "hook/vkGetBufferMemoryRequirementsReceiverHook.hpp"
#endif
#if __has_include("hook/vkAllocateMemoryReceiverHook.hpp")
    #include "hook/vkAllocateMemoryReceiverHook.hpp"
#endif
#if __has_include("hook/vkFreeMemoryReceiverHook.hpp")
    #include "hook/vkFreeMemoryReceiverHook.hpp"
#endif
#if __has_include("hook/vkBindBufferMemoryReceiverHook.hpp")
    #include "hook/vkBindBufferMemoryReceiverHook.hpp"
#endif
#if __has_include("hook/vkMapMemoryReceiverHook.hpp")
    #include "hook/vkMapMemoryReceiverHook.hpp"
#endif
#if __has_include("hook/vkUnmapMemoryReceiverHook.hpp")
    #include "hook/vkUnmapMemoryReceiverHook.hpp"
#endif
#if __has_include("hook/vkFlushMappedMemoryRangesReceiverHook.hpp")
    #include "hook/vkFlushMappedMemoryRangesReceiverHook.hpp"
#endif
#if __has_include("hook/vkInvalidateMappedMemoryRangesReceiverHook.hpp")
    #include "hook/vkInvalidateMappedMemoryRangesReceiverHook.hpp"
#endif

namespace vkfwd::receiver::generated {

bool vkCreateBuffer_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                             ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkCreateBuffer::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::CreateBuffer>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::CreateBuffer);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkCreateBuffer>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device, parameters->pCreateInfo, parameters->pAllocator, parameters->pBuffer);
        Command::Response response {.return_value = return_value, .pBuffer = parameters->pBuffer};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkDestroyBuffer_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                              ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkDestroyBuffer::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::DestroyBuffer>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::DestroyBuffer);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkDestroyBuffer>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->buffer, parameters->pAllocator);
        return true;
    }
}

bool vkGetBufferMemoryRequirements_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                            ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkGetBufferMemoryRequirements::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::GetBufferMemoryRequirements>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::GetBufferMemoryRequirements);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkGetBufferMemoryRequirements>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->buffer, parameters->pMemoryRequirements);
        Command::Response response {.pMemoryRequirements = parameters->pMemoryRequirements};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkAllocateMemory_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                               ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkAllocateMemory::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::AllocateMemory>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::AllocateMemory);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkAllocateMemory>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device, parameters->pAllocateInfo, parameters->pAllocator, parameters->pMemory);
        Command::Response response {.return_value = return_value, .pMemory = parameters->pMemory};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkFreeMemory_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                           ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkFreeMemory::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::FreeMemory>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::FreeMemory);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkFreeMemory>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->memory, parameters->pAllocator);
        return true;
    }
}

bool vkBindBufferMemory_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                 ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkBindBufferMemory::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::BindBufferMemory>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::BindBufferMemory);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkBindBufferMemory>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device, parameters->buffer, parameters->memory, parameters->memoryOffset);
        Command::Response response {.return_value = return_value};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkMapMemory_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                          ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkMapMemory::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::MapMemory>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::MapMemory);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkMapMemory>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult return_value =
            api_function(parameters->device, parameters->memory, parameters->offset, parameters->size, parameters->flags, parameters->ppData);
        Command::Response response {.return_value = return_value, .ppData = parameters->ppData};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkUnmapMemory_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                            ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkUnmapMemory::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::UnmapMemory>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::UnmapMemory);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkUnmapMemory>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        api_function(parameters->device, parameters->memory);
        return true;
    }
}

bool vkFlushMappedMemoryRanges_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                        ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkFlushMappedMemoryRanges::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::FlushMappedMemoryRanges>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::FlushMappedMemoryRanges);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkFlushMappedMemoryRanges>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device, parameters->memoryRangeCount, parameters->pMemoryRanges);
        Command::Response response {.return_value = return_value};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

bool vkInvalidateMappedMemoryRanges_endpoint(const CommandStream & request_stream, const Range & request_range, CommandStream & response_stream,
                                             ::vkfwd::receiver::ReplayContext & replay_context) {
    using Command = ::vkfwd::generated::commands::vkInvalidateMappedMemoryRanges::Command;
    using Hooks   = ::vkfwd::receiver::manual::CommandHooks<::vkfwd::generated::CommandId::InvalidateMappedMemoryRanges>;

    if constexpr (Hooks::replace_endpoint_enabled) {
        // Full override: the endpoint has no standard unpack/call/pack body
        // (e.g. memory-map delegation where no real Vulkan call happens here).
        return Hooks::replace_endpoint(request_stream, request_range, response_stream, replay_context);
    } else {
        const auto raw_function = replay_context.dispatch.getProcByCommandId(::vkfwd::generated::CommandId::InvalidateMappedMemoryRanges);
        if (!raw_function) { return false; }
        const auto api_function = reinterpret_cast<PFN_vkInvalidateMappedMemoryRanges>(raw_function);

        auto & mutable_request_stream = const_cast<CommandStream &>(request_stream);
        auto   request_view           = mutable_request_stream.at(request_range.offset, request_range.size);

        if constexpr (Hooks::before_unpack_enabled) { Hooks::before_unpack(request_view, replay_context); }

        const Command::Parameters * parameters = nullptr;
        if (Command::unpack_parameters(request_view, &parameters) != VK_SUCCESS) { return false; }

        if constexpr (Hooks::before_call_enabled) { Hooks::before_call(*const_cast<Command::Parameters *>(parameters), replay_context); }
        const VkResult    return_value = api_function(parameters->device, parameters->memoryRangeCount, parameters->pMemoryRanges);
        Command::Response response {.return_value = return_value};
        if constexpr (Hooks::after_call_enabled) { Hooks::after_call(*parameters, response, replay_context); }
        if constexpr (Hooks::before_pack_response_enabled) { Hooks::before_pack_response(*parameters, response, replay_context); }
        const bool packed_ok = Command::pack_response(response_stream, response) == VK_SUCCESS;
        if constexpr (Hooks::after_pack_response_enabled) { Hooks::after_pack_response(*parameters, response_stream); }
        return packed_ok;
    }
}

} // namespace vkfwd::receiver::generated
