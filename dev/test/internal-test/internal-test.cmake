# This manifest is consumed by the internal-test target in the same directory.
# Keep entries relative so individual test groups can move without central
# source-list churn.
set(VKFWD_INTERNAL_TEST_LOCAL_SOURCES
  protocol_test.cpp)
